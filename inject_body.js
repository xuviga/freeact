(() => {
  const editors = document.querySelectorAll('[contenteditable]');
  const body = editors[1];
  body.innerHTML = '<h2>The Problem</h2><p>Every Selenium/Playwright script leaves detectable traces: <code>navigator.webdriver === true</code>, missing plugins, headless mode artifacts. Anti-bot systems catch them instantly.</p><h2>The Solution</h2><p>freeact controls your REAL browser — the same Yandex/Chrome/Edge you use daily — through the Chrome DevTools Protocol. The browser doesn\'t know it\'s being automated.</p><p>Unlike Selenium/Playwright, there are <strong>zero automation flags</strong>. Anti-bot systems (CloudFront, reCAPTCHA, Cloudflare Turnstile) cannot detect it.</p><h2>Key Features</h2><ul><li>Uses your <strong>REAL browser profile</strong> with real cookies</li><li><strong>15-layer stealth patch system</strong> (webdriver deletion, canvas noise, WebGL spoofing)</li><li><strong>Free multi-strategy CAPTCHA solver</strong> (audio, OCR, behavioral)</li><li><strong>Daemon mode</strong> — browser stays alive between commands</li><li><strong>Network monitoring</strong> — zero-overhead fetch/XHR interception</li><li><strong>Live browser connection</strong> — attach to your daily browser with all your logins</li><li><strong>Cross-platform</strong>: Windows, macOS, Linux</li></ul><h2>Quick Start</h2><pre>pip install freeact-cli==0.4.0\nfreeact daemon start\nfreeact --session demo browser open DSYandex https://example.com\nfreeact --session demo state\nfreeact --session demo click 1</pre><h2>Under the Hood</h2><p>15 stealth layers, real browser profiles, hardlink-based profile copying, local turndown.js for markdown conversion, adaptive CDP connection, page cache for instant commands.</p><p>Open source (MIT), 14 tests, 0 ruff errors.</p><p><a href="https://github.com/xuviga/freeact">github.com/xuviga/freeact</a></p>';
})()
